[buildout]
extends = base.cfg
develop = .

eggs =
    frozensearch [test]

parts +=
    pyscripts
    robot
    test


[pyscripts]
recipe = zc.recipe.egg:script
eggs = ${buildout:eggs}
interpreter = py
dependent-scripts = true


[robot]
recipe = zc.recipe.testrunner
eggs = ${buildout:eggs}
defaults = ['--color', '--auto-progress', '--layer', 'frozensearchRobotLayer']


[test]
recipe = zc.recipe.testrunner
eggs = ${buildout:eggs}
defaults = ['--color', '--auto-progress', '--layer', 'frozensearchTestLayer', '--layer', '!frozensearchRobotLayer']
